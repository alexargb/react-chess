import styled from 'styled-components';
import { SmallTitle } from '~/components/atoms/title';

export const GameTitle = styled(SmallTitle)`
  padding: 8px 4px;
  margin-top: 16px;
`;

export const GameDiv = styled.div`
  height: fit-content;
`;
